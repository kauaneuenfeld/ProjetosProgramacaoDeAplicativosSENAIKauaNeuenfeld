info:
  name: TestePost
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3023/cursos
  body:
    type: json
    data: |-
      {
        "nome": "C"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
