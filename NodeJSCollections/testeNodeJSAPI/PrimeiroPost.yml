info:
  name: PrimeiroPost
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3055/curso
  body:
    type: json
    data: |-
      {
        "nome": "Python"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
