info:
  name: TestePut
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:3023/cursos/2
  body:
    type: json
    data: |-
      {
        "nome": "DS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
