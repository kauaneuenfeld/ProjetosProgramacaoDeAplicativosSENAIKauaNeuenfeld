info:
  name: TestePost
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3009/destinos
  body:
    type: json
    data: |-
      {
        "nome_destino": "Pomerode",
        "pais_destino": "Brasil"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
